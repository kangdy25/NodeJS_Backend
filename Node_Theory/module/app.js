const { add: addMath, mult } = require('./math');
const { add: addVector, sub } = require('./vector');
require('./fileA');
require('./fileB');

// console.log(addMath(1, 2));

// console.log(
//   addVector(
//     { x: 1, y: 2 },
//     { x: 3, y: 4 }
//   )
// );
